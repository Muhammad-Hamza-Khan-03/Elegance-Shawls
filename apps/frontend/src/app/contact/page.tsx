import { Metadata } from 'next';
import Link from 'next/link';
import { InfoPage } from '@/components/layout/InfoPage';
const digits = (process.env.NEXT_PUBLIC_WHATSAPP_NUMBER || '').replace(/\D/g, '');
export const metadata: Metadata = { title: 'Contact', description: 'Contact Elegance Shawls about products and orders.' };
export default function ContactPage() { return <InfoPage eyebrow="Customer care" title="Contact us" intro="For product questions and order support, use WhatsApp so the conversation stays connected to your selections."><section><h2>Start an enquiry</h2><p>The best route is to open a product, choose its available option and quantity, then select “Order on WhatsApp”. This prepares the relevant product details for you.</p><p>{digits.length >= 10 && digits.length <= 15 ? <a href={`https://wa.me/${digits}`} target="_blank" rel="noopener noreferrer" className="font-semibold text-[#765033] underline">Open WhatsApp directly</a> : <Link href="/products" className="font-semibold text-[#765033] underline">Browse products to start</Link>}</p></section><section><h2>Existing order</h2><p>Reply in your existing WhatsApp order conversation and include the product or order details relevant to your question.</p></section><section><h2>Safety</h2><p>Do not share passwords, one-time codes or unnecessary sensitive information. Confirm payment instructions inside your active order conversation.</p></section></InfoPage>; }
